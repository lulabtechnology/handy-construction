# Email via Resend (recomendado en Vercel)
RESEND_API_KEY=
CONTACT_TO=mi-correo@dominio.com

# Fallback SMTP (opcional; se usa si no hay RESEND_API_KEY)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Público
NEXT_PUBLIC_WHATSAPP=67563200
