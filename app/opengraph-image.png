import { ImageResponse } from "next/og";

export const size = { width: 1200, height: 630 };
export const contentType = "image/png";

export default function OGImage() {
  return new ImageResponse(
    (
      <div
        style={{
          width: "100%",
          height: "100%",
          display: "flex",
          flexDirection: "column",
          justifyContent: "center",
          padding: "64px",
          background:
            "linear-gradient(135deg, #0F2B46 0%, #17456F 60%, #1F7A8C 100%)",
          color: "white",
        }}
      >
        <div style={{ fontSize: 56, fontWeight: 800, lineHeight: 1.1 }}>
          Handy Construction S.A
        </div>
        <div style={{ marginTop: 16, fontSize: 28, opacity: 0.9 }}>
          CONSTRUYENDO EL FUTURO, PROTEGIENDO EL PRESENTE.
        </div>
        <div style={{ marginTop: 28, fontSize: 22, opacity: 0.9 }}>
          Diseño, construcción, capacitación y consultoría
        </div>
      </div>
    ),
    size
  );
}
